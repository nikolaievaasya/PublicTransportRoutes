<UserControl x:Class="PublicTransportRoutes.View.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PublicTransportRoutes.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>

        <StackPanel
            Orientation="Vertical"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">


            <TextBlock
                Text="Public Transport Routes"
                Style="{StaticResource FirstTextStyle}"
                FontSize="40"/>

            <Border
                Background="{StaticResource FirstColor}"
                CornerRadius="3"
                Height="3"/>

            <TextBlock
                Text="System of information about public transport routes."
                HorizontalAlignment="Center"
                Style="{StaticResource FirstTextStyle}"/>


        </StackPanel>

    </Grid>
</UserControl>
